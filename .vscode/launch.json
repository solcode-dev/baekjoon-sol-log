{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "CodeLLDB: Build and Debug Active File",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/build/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "build"
    }
  ]
}